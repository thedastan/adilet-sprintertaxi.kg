import React from 'react';

import Home from './home/Home';
import { Box } from '@chakra-ui/react';

const Hero = () => {
    return (
        <Box>
            <Home/>
        </Box>
    );
};

export default Hero;